spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hr
    username: hr
    password: hr
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
logging:
  level:
    root: DEBUG
    vnedraid.inputservice: DEBUG
    reactor.netty.http.client: DEBUG

hh:
  delay:
    ms: 60000
  monitoring:
    requests:
      - text: "Java Developer"
        area: 1

logging.level:
  vnedraid.inputservice: INFO
